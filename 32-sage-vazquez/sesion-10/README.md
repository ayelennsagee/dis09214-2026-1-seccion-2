# sesión 10 - 12/06
---

# PASO 2
## Crear un evento windowResized 
### Es una función especial que se ejecuta automáticamente cada vez que cambia el tamaño de la ventana del navegador.
# Paso 3
## Usar valores relativos 

el width y el heigth usarlos para que se acomode directamente al tamaño de la pantalla, considerando que para el examen parte de los rerquisitos que es podamos jugar con eso, que nuestro lienzo se ajuste al atamaño que desee el usuario
(width/2, heigth/2);
Basicamente empezar a pensar y usar proporcionalmente los valores de posición. Usando porcentajes o grados.
