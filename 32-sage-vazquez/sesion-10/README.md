# sesión 10 - 12/06
---
En esta clase aprendimos la importancia de crear proyectos que puedan adaptarse a distintos tamaños de pantalla. Para lograrlo, trabajamos con el concepto de diseño responsivo, entendiendo que los elementos no deben ubicarse con valores fijos, sino utilizando proporciones relacionadas con width y height. También vimos cómo utilizar windowResized() para que el lienzo se ajuste automáticamente cuando la ventana cambia de tamaño.

Al profundizar en el diseño responsivo, se introdujo el uso de una variable de referencia mediante min(width, height), la cual permite mantener proporciones más consistentes entre pantallas horizontales y verticales. Además, se recomendó el uso de translate(), push() y pop() para organizar mejor la posición de los elementos en proyectos más complejos.

Trabajo con imágenes

Posteriormente, la clase se enfocó en el uso de imágenes dentro de p5.js. Aprendimos el proceso completo:

Subir imágenes al editor y organizarlas dentro de una carpeta de recursos.
Declarar variables globales para almacenarlas.
Utilizar preload() para cargarlas antes de ejecutar el programa.
Dibujarlas en el lienzo mediante la función image().
Ajustar su tamaño de forma responsiva utilizando ancho y alto variables.
Manipulación de píxeles

Finalmente, se introdujo una parte más avanzada relacionada con la manipulación de imágenes a nivel de píxel. Se explicó cómo funciona loadPixels(), que permite acceder directamente a la información visual de una imagen o del lienzo. También revisamos funciones como:

get() para leer el color de un píxel.
set() para modificar un píxel específico.
updatePixels() para actualizar y mostrar los cambios realizados.

Para cerrar, vimos ejemplos de distorsión de imágenes y generación de patrones visuales, comprendiendo cómo estas herramientas pueden utilizarse para desarrollar experiencias visuales más experimentales dentro del creative coding.

Alcanzaste el límite de Free para chats con archivos adjuntos
Mejora el plan ahora o espera hasta 4:57 a.m. para seguir usando archivos o chatea ahora sin usarlos.
Nuevo chat
Reclamar oferta gratis
